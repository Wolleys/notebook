/**
 * Featured writers interface.
 */
export interface IFeaturedWriters {
  /**
   * Featured writer's id.
   */
  id: number;
  /**
   * Featured writer's name.
   */
  name: string;
  /**
   * Featured writer's church.
   */
  church: string;
  /**
   * Featured writer's username.
   */
  username: string;
}
