import { IIcon } from "./IIcon";

export interface FontAwesomeIconProps {
  /**
   * Icon interface.
   */
  icon: IIcon;
  /**
   * Allow custom class name from styles.
   */
  customClass?: string;
}
